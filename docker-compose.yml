services:
  linkbot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      - OPTIMIZED_POLICY_JSON=/app/optimized_policy.json
    read_only: true
    tmpfs:
      - /tmp

